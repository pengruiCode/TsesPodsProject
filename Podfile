platform :ios, '8.0'
inhibit_all_warnings!

def pods

pod 'SQRNetworkRequset',            :git => 'https://github.com/pengruiCode/SQRNetworkRequset.git'

pod 'LocalFiles',            :path => '../LocalFiles'

end

target 'TsesPodsProject' do
    pods
end
